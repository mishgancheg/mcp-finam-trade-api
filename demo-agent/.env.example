# Claude API
ANTHROPIC_API_KEY=your_api_key_here
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# MCP Server
# For stdio transport (spawns MCP server process):
# MCP_SERVER_URL=stdio://path/to/finam-mcp-server
# or stdio://../dist/src/mcp/index.js

# For HTTP/SSE transport (connects to running MCP server):
# MCP server runs on port 3001 by default
MCP_SERVER_URL=http://localhost:3001

# MCP Server Configuration (passed to stdio transport)
# Enable endpoint visibility in MCP responses
SHOW_MCP_ENDPOINTS=false

# API Server
WEB_API_PORT=3002
WEB_API_HOST=localhost

# Agent Configuration
AGENT_MAX_TURNS=10
AGENT_TIMEOUT=30000
AGENT_MODE=streaming

# Демо работает в связке с MCP сервером, запущенным на транспорте HTTP
# В таком случае требуются секретный ключ для авторизации, который передается
# в заголовках запросов к MCP: Authorization: Bearer <secret-token>
#
# Секретный ключ можно ввести в UI в поле "Секретный ключ" (приоритет)
# или указать здесь в переменной окружения API_SECRET_TOKEN (резервный вариант)
#
# Если секретный ключ не задан ни в UI, ни в переменной окружения,
# то при попытке отправить сообщение появится ошибка
API_SECRET_TOKEN=<secret-token>
